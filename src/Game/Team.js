class Team {
	constructor({ iso, teamState }) {
		// this.iso = iso;
		// this.position = [0, 0];
		// this.teamState = teamState;
		this.medals = [];
	}

	move(direction) {}

	collect() {}

	get score() {
		return 0;
	}
}

export { Team };
